name: Staging — Regression Test & Auto-Promote

on:
  push:
    branches: [staging]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          # MUST match the production venv (3.10). CI on 3.12 let an
          # f-string backslash — legal from 3.12, a SyntaxError before
          # it — reach production and take the API down on 2026-09-16.
          # If the server is upgraded, change it here in the SAME PR.
          python-version: '3.10'
      - run: pip install -r requirements.txt
      - name: Run all tests (full regression)
        run: pytest tests/ -x --ignore=tests/integration -v
        env:
          FLASK_ENV: testing
          PYTHONPATH: .

  promote:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Auto-merge staging → deployment
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin deployment
          git checkout deployment
          git merge origin/staging --no-ff -m "Auto-promote: staging tests passed"
          git push origin deployment
      - name: Trigger deploy workflow
        run: gh workflow run deploy.yml --ref deployment
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
